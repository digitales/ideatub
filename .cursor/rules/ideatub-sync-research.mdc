---
description: When saving research agent output (or any research) to IdeaTub via MCP capture_plan
globs: "**/research/**/*.md"
alwaysApply: false
---

# IdeaTub: Save research via capture_plan

When the user or a research agent has research output to save to IdeaTub, use the MCP tool **capture_plan** with:

- **content** (required): The research text (full output or one section). The server cannot read local paths; send the text.
- **doc_type**: `research`.
- **plan_slug**: A short slug for this research run (e.g. `2026-03-13-vehicle-valuation`, `competitor-analysis-Q1`). Same slug for all sections of the same research so Stream can show them together via tag `research:<slug>`.
- **project**: The **research project** or topic name (e.g. workspace/repo name, or the research question: `vehicle-valuation`, `ideatub-mcp`). Use a consistent value for the whole research save so thoughts are tagged with which project they belong to. Stored in source_metadata.
- **file_path**: Optional; if research was read from a file, use repo-relative path (e.g. `research/2026-03-13-vehicle-valuation.md`). Otherwise omit or use a logical path.
- **section_title**: For each section, the heading or a short title (e.g. "Summary", "Findings", "Sources").

**Preferred: one call with full content.** Send the **entire research** in a **single** capture_plan call (no `parent_id`). IdeaTub will auto-chunk at markdown headings so the research appears as a top-level thought with section children. Do not create a root thought first and then attach the full document as one child — that produces a blank card and one unchunked entry.

**Alternative: one call per section.** If you split at section titles yourself, call capture_plan once per section **without** `parent_id`, in order, with the same `doc_type`, `plan_slug`, and `project`. Do not send the full document in one call with a parent_id.

**Stream long-form view:** In IdeaTub, open Stream and filter by tag, e.g. `/stream?tag=research-2026-03-13-vehicle-valuation`.

IdeaTub MCP must be configured (MCP key, server URL). See `docs/cursor-mcp-integration.md`, `docs/mcp-integration-guide.md`, or Help in the app.
